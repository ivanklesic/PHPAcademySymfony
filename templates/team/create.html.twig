{% extends 'base.html.twig' %}

{% block title %}{% if not edit %}Create new team {% else %} Edit team {% endif %}{% endblock %}

{% block body %}

    <div class="row justify-content-center">
        <h3 class="h3 mb-3 font-weight-normal">{% if not edit %}Create new team {% else %} Edit team {% endif %}</h3>
    </div>

    <div class="row justify-content-center">
        <div class="col-6">
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.members) }}
            <button type="submit" class="btn btn-block btn-primary">Submit</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    <script>

    </script>
{% endblock %}
